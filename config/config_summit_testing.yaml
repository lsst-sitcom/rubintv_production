# Required items:
dataIdScanPath: '/home/mfl/rubintv/rubintv/dataIds'
bucketName: 'rubintv_data'
dimensionUniverseFile: '/home/mfl/rubintv/rubintv/butlerDimensionUniverse.json'

# Summit items:
nightReportPath:     '/home/mfl/rubintv/rubintv/nightReportData'

# AuxTel metadata
auxTelMetadataPath: '/home/mfl/rubintv/rubintv/sidecar_metadata'
auxTelMetadataShardPath: '/home/mfl/rubintv/rubintv/sidecar_metadata/shards'

# all sky
allSkyRootDataPath: '/project/rubintv/allsky/raw'
allSkyOutputPath: '/home/mfl/rubintv/rubintv/allsky'

# animation
moviePngPath: '/home/mfl/rubintv/rubintv/auxTel_animation'

# Star Trackers paths:
# The path at which to find the data on disk. Does not include the
# GenericCamera/101/ or GenericCamera/102/ part, just the base directory.
starTrackerDataPath: '/project'

# for writing the per-day json files and shards to for the star tracker channels
starTrackerMetadataPath: '/home/mfl/rubintv/rubintv/starTracker/sidecar_metadata'
starTrackerMetadataShardPath: '/home/mfl/rubintv/rubintv/starTracker/sidecar_metadata/shards'

# for writing the rendered pngs of the a.net fits out to
starTrackerOutputPath: '/home/mfl/rubintv/rubintv/starTracker/output'

# The path to the astrometry.net reference catalogs. Do not include
# the /4100 or /4200, just the base directory.
astrometryNetRefCatPath: '/project/shared/ref_cats/astrometry_net'
